#!/bin/bash
echo "Starting ASC-Scheduler React Frontend on port 5173..."

# Install dependencies if not already installed (npm is standard for Vite)
npm install

# Start the Vite development server
npm run dev